# Implementation status

Evidence date: 14 September 2026. Version: 0.1.0-prototype; no production release tag.

**Stage-0 assessment is complete: NO-GO for advancement under the Workers Free CPU requirement.** Runtime `17d0a30f68254394a81946c0351403a8b0d83dbd` passed 192 core and 22 Workerd tests, Linux CI and actual ChatGPT/Codex diagnostic checks, but sampled CPU overruns remain. See the [final assessment and requirement audit](stage-zero-assessment.md). Production and Jules-key setup remain gated. The sections below retain the dated investigation history; earlier pending items are superseded by the final assessment. Continued from preserved public main `c6960f1486bc2616dd3e7f1f17a7856e09f35087`.

## Acceptance criteria and evidence

| Claim / acceptance criterion                       | Evidence and disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Preserve the 13-tool contract and isolation        | Schemas unchanged. Runtime changes map expected owner-authorization validation errors to sanitized 400, preserve server failures, and set strict-origin only on successful GET authorization pages so form POSTs can retain their exact Origin. Policy lint and schema baseline pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Stage 0 works without a Jules key or outbound HTTP | Strengthened existing Workerd test deletes JULES_API_KEY and spies on fetch; discovery, nonce write and read-back pass with zero HTTP calls. Local KV and synthetic credentials only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Reproducible local validation                      | pnpm 10.13.1 frozen install and full check passed in an isolated Windows worktree: 192 core tests, 19 Workerd tests, zero failed/skipped, formatting, typecheck, lint, schema, history credential scan and both dry runs. Production dependency audit found no known vulnerabilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Baseline has independent Linux CI                  | [Run 34876093754](https://github.com/profplum700/jules-mcp/actions/runs/34876093754) re-fetched: both jobs passed for c6960f1486bc2616dd3e7f1f17a7856e09f35087. Continuation [run 34902366178](https://github.com/profplum700/jules-mcp/actions/runs/34902366178) passed both jobs, frozen install, full check and audit for 5c9fccba00b49712603f8ba1242e84ddbe405852. Preservation follow-up [run 34902645175](https://github.com/profplum700/jules-mcp/actions/runs/34902645175) also passed for b634f85eabb482544967ee8ca2d73ea4c48fa6c9. Consent-fix [run 34903129648](https://github.com/profplum700/jules-mcp/actions/runs/34903129648) passed both jobs for runtime 05f687854eb8b4b12e738e9be1b4bf2d9db0387b. Later evidence/test edits need their own CI. |
| Actual account is Workers Free                     | Authenticated dashboard explicitly displayed Free as Current plan. No upgrade. Plugin subscription read lacked permission; dashboard supplied account evidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Native limiters and auth KV deploy on Free         | Dedicated Stage-0 KV created and re-fetched. All three native limiter bindings accepted; script settings and workers.dev route independently re-fetched. No existing Worker changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Deployment reachable and fails closed              | Baseline runtime bundled by Wrangler and uploaded through authorized Cloudflare plugin. Initial health returned 200 and protected routes 503 CONFIGURATION_ERROR; that proved incomplete-config rejection, not the disable switch. After secrets deployment and enabling, live authenticated operations passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Owner identity and separate credentials            | Stable owner ID verified through authenticated GitHub API. Owner ID, admin verifier and separate service-token registry applied through Cloudflare secrets API; secret names re-fetched. Raw credentials remain in a Windows-ACL-restricted private directory. User completed GitHub OAuth registration and saved secrets; the saved version was deployed and secret names independently verified. No Jules key entered.                                                                                                                                                                                                                                                                                                                                          |
| Ordinary ChatGPT web feasibility                   | Personal Pro web OAuth connection completed with the pre-registered confidential client and client_secret_post. Refresh discovered exactly the diagnostic read and write tools. ChatGPT Pro executed read({}), one write({}), and immediate read of its returned nonce: authMethod oauth, both scopes, productionGateway false, nonceFound changed false to true. Tool activity is visible in the conversation. Reconnect, refresh-token use and OAuth grant revocation remain unverified.                                                                                                                                                                                                                                                                        |
| Coding clients                                     | Installed Codex CLI 0.154.0, Claude Code 2.1.270, OMP 18.1.18 versions executed. Actual Codex CLI 0.154.0 authenticated over HTTPS using a dedicated bearer and ephemeral config: write with empty arguments, then returned nonce read-back true. Initial invalid-argument trial was explicitly rejected without mutation; corrected schema-valid trial passed. Claude/OMP not tested.                                                                                                                                                                                                                                                                                                                                                                            |
| Production gates                                   | No GitHub environments and main unprotected at inspection. Protected trusted-main deployment setup and successful Stage 0 remain required. No production deployment, Jules task, PR merge or release tag.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Free measurements

Dashboard showed 1,978 of 100,000 account-wide requests used that day before this trial. Current [Workers limits](https://developers.cloudflare.com/workers/platform/limits/) confirm 10 ms CPU/request, 128 MB memory and 50 subrequests on Free. [KV pricing](https://developers.cloudflare.com/kv/platform/pricing/) confirms 100,000 reads/day, 1,000 writes, deletes and list operations/day each, and 1 GB storage. Later account-wide GraphQL usage reported 72 KV reads, 26 writes, 8 deletes and 2 lists; diagnostic storage was 5,833 bytes and 9 keys. The account had 2,183 Worker requests. These are sampled/lagged observations. The later dashboard snapshot included 32 invocations across initial and enabled versions: CPU P50 2.52 ms / P90 8.23 ms / P99 10.11 ms, memory P99 10.27 MB, zero reported invocation errors or CPU-limit terminations. The CPU tail is at the Free allowance; this small mixed-route sample is not OAuth/cold-warm qualification or a performance pass.

Initial Cloudflare startup was 48 ms; the deployed consent-fix runtime reported **55 ms startup**, with 784.40 KiB upload / 183.14 KiB gzip measured by Wrangler. Initial local probes took 519 ms (health), 284 ms (OAuth metadata), and 177 ms (MCP), including network time. These are not authenticated request CPU, cold/warm qualification or proof OAuth fits Free. Native binding acceptance is now observed; approximate enforcement and quota exhaustion are distinct risks.

## Verification lanes

- Static/build, unit/regression, schema/conformance and synthetic security/concurrency: baseline and pre-runtime continuation passed locally. All 19 Workerd tests passed after the authorization fix. Full pnpm check passed after the authorization fix; continuation CI must use the published candidate.
- Real service: live SDK exact diagnostic tool surface, write/read-back, cross-credential nonce isolation and read-only denial passed. After removing one service credential, it returned 401 and the retained client returned 200 from the local route. Selected OAuth grant revocation removed the grant and caused hosted ChatGPT to require reconnection; the independent service remained usable. Jules remains unverified.
- Browser: actual Cloudflare plan, ChatGPT OAuth connection and diagnostic read/write/read-back verified. Natural-expiry refresh, revocation denial and returning-user reconnect passed; Free CPU qualification remains open.
- Performance: current-runtime interval had 27 requests, zero runtime errors, CPU P50 4.735 ms and P99 24.494 ms. The tail exceeds the 10 ms Free target; per-operation attribution, profiling and fresh qualification remain required. Account KV/request/storage observations are recorded above.
- Recovery: the configured kill switch returned protected503 GATEWAY_DISABLED and health200. Version rollback restored the verified runtime and SDK read; revoked-service401 and retained-service200 were preserved. Deployment version, rather than the settings endpoint that showed latest-uploaded configuration, identified active traffic. See [operations](operations.md); no secret or KV rollback is claimed.
- Golden/visual regression, additional fuzz/property and metamorphic lanes: not selected for the narrow exception mapping; no visual artifact, parser algorithm or schema changed. Workerd regressions cover unknown-client 400, absent redirect/reflection, retained storage-failure 500, and existing successful OAuth exchange. Re-evaluate if authorization semantics or parsing changes.
- Migration/payment: no migration, payment operation or paid resource introduced. Re-evaluate if scope changes.
- Fresh-context review: separate ChatGPT 6 Pro review completed after 11 minutes with GitHub source and pinned OAuth-provider inspection. Verdict: suitable for controlled disabled feasibility preparation, not Stage-0 acceptance or production. The no-key evidence gap and two configuration defects were addressed; a completed 2m28s follow-up found all three exact changes sound for non-release publication. It explicitly did not approve an enabled trial or production. The reviewer did not independently run tests.

## Independent review dispositions

The renderer now rejects production Worker names ending in -feasibility, including default production mode. Its regression checks both cases. The deploy workflow now passes the optional ALLOWED_ORIGINS_JSON setting with an empty-array default. The no-key Workerd test asserts zero outbound HTTP. These are bounded fixes; they do not establish full cross-environment isolation or hosted OAuth authentication. Live unknown-client handling additionally exposed a 500 mapping defect: reproduced red in Workerd, corrected to sanitized OAUTH_INVALID_REQUEST 400 in the provider adapter. A separate regression preserves 500 for storage failures without disclosing error text. A completed 2m54s Pro review accepted the mapping for non-release publication subject to preservation coverage. The expanded Workerd test now covers both typed provider server errors, an unexpected storage error and an existing owner-denial error; all 18 tests pass. Generic local 400 handling is not complete OAuth error-redirect interoperability.

Open release blockers from review: enforce protected branch/environment and a reviewed Stage-0 promotion record; verify distinct production KV, origins, limiter IDs and credentials; resolve the pinned provider's previous-refresh-token reuse semantics before accepting public OAuth clients; assess pre-owner-login KV writes against the daily quota; make acceptance smoke tests require the intended deployment/tool surface and version; and measure real-client refresh/revocation and authenticated CPU/KV behavior. Do not enable a public-client trial or approve production on the basis of these local fixes. The reviewed refresh concern is not a demonstrated PKCE bypass or live exploit. KV consistency prevents an instantaneous global revocation claim.

## Remaining engineering

Complete attributed CPU profiling and Free-envelope qualification; ChatGPT initial login, discovery, diagnostic read/write/read-back, natural-expiry refresh, grant revocation and returning-user reconnect are now observed. Measure CPU/KV and exercise rollback. Configure protected GitHub deployment settings. Production remains GitHub Actions to Wrangler on trusted main after validation; plugin upload was only Stage-0 bootstrap. Then test Jules reads; actual Codex CLI Stage-0 bearer testing is complete. Live mutations await an explicitly designated disposable repository.

## Remaining human actions

GitHub owner-login registration and provider secret entry are complete. The corrected browser flow now reaches GitHub sign-in. Personal GitHub login and the initial ChatGPT diagnostic round trip are complete; no immediate human action is required for those checks. The GitHub OAuth credentials belong only in Cloudflare. Supply Jules key privately only after Stage 0 passes; designate a disposable repository before mutations. Never paste secrets into chat.

Local Wrangler refresh reached consent but expired; it is not required for the plugin-based upload already completed. A scoped GitHub Actions deployment credential is still required for production.

## Reproduction and provenance

Executed pnpm install --frozen-lockfile, pnpm check, pnpm audit --prod --audit-level=high, pnpm deploy:config, and private-config Wrangler dry runs including minification with --outfile. Current Wrangler --outfile contains multipart upload data. Initial API upload incorrectly nested that body as JavaScript and was rejected with error 10021; independent lookup confirmed no Worker existed. Uploading the original multipart body with its own boundary succeeded. No ambiguous mutation was retried.

Original checkout has a pre-existing untracked logs directory that failed formatting; it was preserved and validation moved to an isolated worktree. Schemas unchanged; deployment rendering/workflow, OAuth validation-error mapping and regression tests were strengthened. Provider origin/IDs/versions are retained privately rather than published in source. No prior live version is claimed as a tested rollback target.

## Browser consent correction

A real browser consent POST returned FORBIDDEN after the correct registered client reached the consent page. Both response layers used no-referrer. Current browser semantics can make non-CORS form POSTs send Origin:null under that policy, conflicting with the required exact-origin check. The final successful GET /authorize response now uses strict-origin, which omits path/query from Referer; all other responses retain no-referrer. Null and foreign consent origins remain denied. Workerd policy expectations were red before the fix and green after it. Browser automation blocked the synthetic form controls, so the actual failing request header was not captured and the causal diagnosis still requires user browser confirmation. No browser protections were disabled.

The completed 3m29s independent Pro review accepted this narrow correction for Stage-0 deployment and a user browser retest. It required successful consent302 and rejected authorization responses to retain no-referrer; those assertions pass, alongside literal-null/foreign-origin rejection. Runtime 05f687854eb8b4b12e738e9be1b4bf2d9db0387b was deployed after its Linux CI passed. Live HTTPS probes verified authorization200 with strict-origin, null-origin403, exact-origin consent302 to GitHub with no-referrer, unknown-client400, and retained SDK read access. The synthetic probe did not follow the GitHub redirect and is not browser login acceptance. All five existing secrets were independently re-fetched by name; no Jules secret exists.

The first fresh user browser attempt after deployment still failed before GitHub, at /consent, with OAUTH_INVALID_REQUEST rather than the previous FORBIDDEN. Subsequent observed browser submissions established the actual redirect failure: the native DOM contains the transaction, POST sends the matching transaction and cookie with the exact Origin, the gateway returns 302 to GitHub, and KV advances to the GitHub phase. Browser policy logging explicitly reports that form-action 'self' blocked the redirect, with ERR_ABORTED. Reusing the consumed consent transaction explains the subsequent invalid-request response. The higher-level read-only DOM view omitted the hidden value; native DOM inspection confirmed its presence without disclosing it.

The consent page now permits the fixed GitHub HTTPS origin in form-action. This exception is restricted to successful authorization HTML; consent and callback responses retain self-only form-action and no-referrer. Exact-origin, cookie binding, expiry, identity and all authentication checks remain unchanged. Acceptance requires a real browser to traverse the redirect, complete owner login, and run the diagnostic tools. Workerd checks the exact authorization CSP and retained consent policy; those header checks alone cannot prove browser navigation. This remains an open browser acceptance lane until the corrected runtime is tested.

The previous documentation/test candidate c6486c1688d76ace237368f8004f16b9fcb291d3 passed both Linux CI jobs in run 34903966390. CSP runtime 303e73b78b1adde0a8cfea10ca70a4ebdbac02ba passed pnpm check (192 core tests, 19 Workerd tests, format/policy/type/schema/history-secret gates and both dry runs) and both Linux CI jobs in run 34904701194. The completed 3m35s independent Pro review accepted the supplied diff for isolated Stage-0 deployment and browser testing, not production. It highlighted a returning-user redirect chain that might reach the external client without a GitHub document; that path remains untested and could require a separate reviewed correction.

After deploying that runtime, an independent provider read-back confirmed the current deployment and five preserved secret bindings, with no Jules key. Live probes verified the exact GitHub form destination allowance, retained strict-origin/no-referrer behavior, exact-origin consent302, null-origin403 and unknown-client400. A fresh real in-app browser consent submission successfully reached GitHub's sign-in page. At that handoff, personal GitHub login blocked further testing. The subsequent owner login and initial diagnostic round trip are recorded below; reconnect and returning-user redirect behavior remain unverified. No browser protections were bypassed. The authenticated CPU/KV, OAuth refresh/revocation and rollback lanes remain open; previous measurements do not qualify this new runtime.

## Hosted ChatGPT diagnostic acceptance

The user completed personal GitHub login. ChatGPT Settings then showed Connected on 14 September 2026 and Authorization used OAuth. Refresh populated exactly jules_feasibility_read and jules_feasibility_write, with correct read/write annotations. A fresh ChatGPT 6 Pro conversation executed read with empty arguments, one write with empty arguments, and immediate read of its returned nonce. The visible tool activity and completed answer reported authMethod oauth, both jules scopes, productionGateway false, a 120-second nonce lifetime and nonceFound false then true. No write retry or Jules call was requested. This is hosted-client diagnostic evidence for deployed runtime 303e73b78b1adde0a8cfea10ca70a4ebdbac02ba, not production Jules acceptance. The documentation-only follow-up 7fbf57a88157f477d35e124db53b231c836a3e87 also passed Linux CI run 34905004106. Remaining lanes are reconnect/returning-user redirects, refresh, OAuth grant revocation, authenticated Free measurements and rollback.

## OAuth lifecycle and returning-user correction

On the deployed 303e73b runtime, ChatGPT read successfully after the initial 900-second access-token lifetime without new login. A provider grant inspection before and after showed previousRefreshTokenId changing from absent to present, corroborating actual refresh rotation without exposing values. Revoking the selected grant through the owner helper left zero matching grants. The next hosted read required Reconnect; the independent service client remained200 and the previously revoked service remained401.

The subsequent returning-user flow traversed consent302, GitHub authorize and gateway callback but was blocked at the external client callback by form-action. The candidate authorizes only the HTTPS origin parsed from the redirect already checked for exact membership in the owner-registered client. Whitespace, semicolons and wildcards are rejected before rendering or transaction creation. No arbitrary query origin, wildcard policy, CORS change or authentication weakening is introduced. Workerd covers wrong-host and same-host wrong-path rejection, registered wildcard/delimiter rejection, exact allowed policy and retained response policies. Deployment and real returning-user verification remain required.

The completed independent CPU assessment explicitly keeps Free qualification open: successful over-budget invocations may reflect enforcement flexibility and do not prove the 10 ms target. Investigate attributed request classes and first-use versus warm samples before optimization; preserve limiters, crypto and request-local principals.

The completed 5m15s independent redirect review accepted the registered-origin approach conditionally on a literal CSP-source guard and regression matrix. The final guard validates every parsed DNS label, rechecks HTTPS/no-userinfo/no-fragment, and preserves explicit numeric ports. Added registered encoded-wildcard, bare-wildcard, semicolon, comma and quote rejection; two-client origin isolation; path/query omission; explicit-port preservation; and callback policy checks. Non-DNS/IPv6 hosted callback forms are explicitly unsupported in this prototype. Full gate and final-candidate CI must pass before deployment.

## Returning-user verification on the final correction

Runtime 70f2008e36540843ccfaeb1f7dd8596e1f770830 passed pnpm check (192 core and 21 Workerd tests) and both Linux CI jobs in run 34907625008, including frozen install and dependency audit. The reviewed guard/matrix conditions were implemented before deployment. Provider read-back confirmed this commit, all five secrets, all three limiters and enabled configuration; no Jules key exists. The browser then completed returning-user authorization through GitHub and back into ChatGPT. A new provider grant was independently observed. ChatGPT resumed its pending read successfully with oauth identity, productionGateway false and a different credential identifier from the revoked connection. The retained SDK read passed; revoked service401/retained service200 were preserved.

Chronology matters: the earlier revoked connection produced a hosted Reconnect card, after which the operator explicitly initiated a new connection. ChatGPT resumed the pending read and described it as success rather than denial. That later response tests the new grant, not the revoked grant. The preceding zero-grant owner read-back and hosted reconnect-required observation remain the revocation evidence. No instantaneous global revocation claim is made.

The remaining Stage-0 blocker is Free qualification. Version-filtered lifecycle observations on the preceding runtime separated refresh-window requests at 5.158/8.394/7.511 ms and the returning GitHub callback at 8.968 ms; a separate service-check bucket reached15.274 ms. These samples do not erase the earlier24.494 ms tail. Profile real Workerd with synthetic inputs, attribute first-use and warm request classes, and repeat deployed measurements on the final candidate. Do not weaken auth, omit native limiters or manufacture cheap requests to dilute the sample.

## Schema-allocation experiment

Version-filtered observations of runtime 70f2008 also include MCP buckets reaching 15.434 and 20.950 ms with zero errors. Local Workerd CPU profiles with synthetic credentials showed setup/schema work, but sampling and first-use effects prevent a reliable improvement claim. The candidate hoists only the two fixed diagnostic input schemas. Servers, handlers, principals, configuration and budgets remain request-local; dependencies and security controls are unchanged.

A completed 3m28s independent Pro review accepted this exact change for isolated diagnostic remeasurement, conditional on unchanged contracts, repeated validation, diagnostic credential isolation and final-candidate gates. The complete diagnostic tools/list definitions from the deployed baseline and local candidate were structurally equal. A focused Workerd regression verifies valid-invalid-valid inputs, rejection before nonce persistence, concurrent A/B identity and nonce isolation, read-only write denial and zero outbound fetch with no Jules key. All 22 Workerd tests passed. The prior full gate passed 192 core plus 21 Workerd tests before this additional regression; the final candidate must pass the full gate and Linux CI before deployment.

The per-flow KV planning estimate is recorded in feasibility.md. It is a source-derived allowance model, not a billing measurement. Cloud CPU comparison and hosted functional smoke on this candidate remain pending; Stage 0 stays open.

Runtime 17d0a30f68254394a81946c0351403a8b0d83dbd subsequently passed the full gate (192 core and 22 Workerd tests) and both Linux jobs in CI run 34909194669. The diagnostic deployment was independently read back with all five secrets and three native limiters, enabled configuration and no Jules key. A bounded 15-request service trial passed initialization, discovery, read, one nonce write and read-back. The existing ChatGPT Pro web OAuth connection again reported successful read/write/read-back with oauth identity, productionGateway false and nonceFound true. Revoked-service 401 and retained-service 200 were preserved.

The experiment has not resolved CPU qualification. Exact-version adaptive observations include 14.879 ms on the first recorded service initialization, 18.883 ms in the separate retained-service check and 17.896 ms in the hosted OAuth window; later sampled requests were approximately 2.4–4.9 ms. All observed outcomes were successful, which does not establish the 10 ms target. These are small adaptive samples, not a stable workload P99; first recorded requests are not proven cold isolates. Raw queries, timestamps and private deployment identity are retained outside Git. No causal performance improvement is claimed.
